#include <stdio.h>
/**
 * main - prints the combo of three nums
 * Return: returns zero
 */
int main(void)
{
	int i, j, k;
	for (i = 0; i <= 9; i++)
	{
		for (i = 1; i <= 9; i++)
		{
			for (i = 2; i <= 9; i++)
			{
				if (i > j && i > k)

			}
		}
	}
}
