#include "main.h"
/**
 * add - prints the result after adding two integers
 * @one: first number
 * @two: second number
 * Return: returns addition
 */
int add(int one, int two)
{
	return (one + two);
}
